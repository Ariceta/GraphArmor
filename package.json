{
  "name": "ORMarketplace",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ asiernexxyotest/ORMarketplace",
    "create-local": "graph create --node http://localhost:8020/ asiernexxyotest/ORMarketplace",
    "remove-local": "graph remove --node http://localhost:8020/ asiernexxyotest/ORMarketplace",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 asiernexxyotest/ORMarketplace"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.4",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
